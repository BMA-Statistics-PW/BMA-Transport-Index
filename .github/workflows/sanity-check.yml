name: Data Sanity Check

on:
  push:
    paths:
      - 'data/**'
      - 'scripts/**'
      - 'src/js/transit-data.js'
  pull_request:
    paths:
      - 'data/**'
      - 'scripts/**'
      - 'src/js/transit-data.js'
  workflow_dispatch:

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run sanity checks
        run: node scripts/sanity_check.mjs

      - name: Validate catalog.json
        run: |
          python3 -c "
          import json, sys
          with open('data/catalog.json') as f:
              catalog = json.load(f)
          assert 'datasets' in catalog, 'catalog.json missing datasets'
          assert len(catalog['datasets']) >= 2, 'catalog.json needs at least 2 datasets'
          print('catalog.json OK —', len(catalog['datasets']), 'datasets found')
          "

      - name: Check CSV files exist and are non-empty
        run: |
          for f in \
            data/ridership/transport_report.csv \
            data/ridership/transport_share.csv \
            data/travel-speed/travel_speed.csv \
            data/travel-speed/source/road_speeds_2568.csv \
            data/travel-speed/source/zone_urban_trend_2560-2568.csv; do
            if [ ! -s "$f" ]; then
              echo "FAIL: $f is missing or empty"
              exit 1
            fi
            echo "OK: $f"
          done
